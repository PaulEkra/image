<script src="https://unpkg.com/axios/dist/axios.min.js"></script>

<header class="p-3 mb-3 border-bottom">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a href="/photos" class="d-flex align-items-center mb-2 mb-lg-0 text-dark text-decoration-none">
          <i class="bi bi-house-fill"></i>
        </a>

        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <li><a href="/photos" class="nav-link px-2 link-secondary">Accueil</a></li>
        </ul>

        <div class="dropdown text-end">
          <% if(user){ %>
          <a href="#" class="d-block link-dark text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
          <img src="/public/uploads/<%= user.avatar %>" alt="Photo de profil" width="32" height="32" class="rounded-circle">
          </a>
          <ul class="dropdown-menu text-small text-center"" aria-labelledby="dropdownUser1">
            <li><div class="text-center text-danger">
              <img src="/public/uploads/<%= user.avatar %>" alt="Photo de profil" width="32" height="32" class="rounded-circle">
              <h4><%= user.username %></h4>
            </div></li>
            <li>
              <form action="/logout" method="post"><button class="btn" type="submit">Déconnexion</button></form>
            </li>
            <% } else { %>
              <a class="btn" href="/login">Connexion</a>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
</header>